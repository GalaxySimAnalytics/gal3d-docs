version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.11"
  jobs:
    post_install:
      - make api
    pre_build:
      - make -C docs compile-translations

python:
  install:
    - requirements: requirements.txt

sphinx:
  configuration: docs/source/conf.py

formats: []